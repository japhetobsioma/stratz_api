name: example
description: A new Flutter project.
publish_to: 'none' 
version: 0.1.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  stratz_api:
    path: ../
  flutter_riverpod: ^1.0.0-dev.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
